import { createMuiTheme } from '@material-ui/core/styles';

const Theme = createMuiTheme({
  typography: {},
  palette: {
    primary: {
      main: '#2abb9b',
    },
    secondary: {
      main: '#ffc244',
    },
  },
});

export default Theme;
